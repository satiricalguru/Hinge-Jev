{
  "name": "hinge-jev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Jev-powered decision workbench that prices uncertainty and asks the question that matters.",
  "scripts": {
    "dev": "tsx server/index.ts",
    "build": "tsc --noEmit && vite build",
    "start": "NODE_ENV=production tsx server/index.ts",
    "test": "tsx --test tests/*.test.ts",
    "format": "prettier --write src server scripts tests README.md docs/*.md *.json *.ts *.html .github/workflows/*.yml",
    "format:check": "prettier --check src server scripts tests README.md docs/*.md *.json *.ts *.html .github/workflows/*.yml",
    "evaluate": "tsx scripts/evaluate.ts",
    "evaluate:live": "tsx scripts/evaluate.ts --live"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.1",
    "prettier": "^3.9.8",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=22"
  }
}
